import { OpenProductDirective } from './open-product.directive';

describe('OpenProductDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenProductDirective();
    expect(directive).toBeTruthy();
  });
});
